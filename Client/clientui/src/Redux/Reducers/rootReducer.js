const InitialState={
    
}

const rootReducer=function(state=InitialState,action){
    switch(action.type){
        case "":{

            break;
        }

        default:{
            return state
        }
    }
}

export default rootReducer